[project]
name = "kanbanano"
version = "0.1.0"
description = "Self-hosted kanban board"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "jinja2>=3.1.6",
    "python-multipart>=0.0.32",
    "uvicorn>=0.53.0",
]
